function Footer() {
    return (
        <footer style={{
            marginTop: "clamp(40px, 8vw, 80px)",
            padding: "clamp(18px, 3vw, 25px)",
            background: "#7A1F1F",
            color: "white",
            textAlign: "center",
            fontSize: "var(--font-body)"
        }}>
            Vachana Sanchaya
        </footer>
    );
}

export default Footer;

